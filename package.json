{
  "name": "merchant-presets",
  "private": true,
  "description": "Build tooling for the Merchant Presets Foundry VTT module",
  "scripts": {
    "pack": "npm run pack:merchants && npm run pack:stock && npm run pack:goods",
    "pack:merchants": "fvtt package pack -n merchants --in _source/merchants --out packs",
    "pack:stock": "fvtt package pack -n stock --in _source/stock --out packs",
    "pack:goods": "fvtt package pack -n goods --in _source/goods --out packs"
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^1.0.4"
  }
}
