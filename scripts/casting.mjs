/**
 * The chat message for a bought spellcasting service (#70). Not yet written:
 * these exports exist so its tests fail on what they check.
 */

export function castsIn(_itemPrices) { return []; }

export function actorEffects(_effects) { return []; }

export function castingMessage(_purchase) { return ""; }

export function chatRecipients(_mode, _gmIds, _userId) { return null; }
